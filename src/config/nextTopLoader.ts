type NextTopLoaderProps = {
  showSpinner: boolean;
  color: string;
  easing: string;
  initialPosition: number;
};

export const nextTopLoaderProps: NextTopLoaderProps = {
  showSpinner: false,
  color: '#847DF9',
  easing: 'ease',
  initialPosition: 0.08,
};
